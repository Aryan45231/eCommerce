<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            color: white;
            overflow: hidden;
        }

        body {
            background-color: black;
            display: flex;
            height: 100vh;
            margin-top: 50px;
            ;
            align-items: flex-start;

            justify-content: center;

        }

        .forget_from_container {
            height: 300px;
            width: 50vh;
            display: flex;

            flex-direction: column;
            align-items: center;
            justify-content: flex-start;

        }

        .forget_from_container * {
            margin-top: 40px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input {
            width: 200pxx;
            font-size: 20px;
            padding: 10px;
            border: none;
            border-bottom: 1px solid white;

            background-color: black;
        }

        input:focus {
            background-color: black;
        }

        button {
            padding: 10px;
            font-size: 20px;
            width: fit-content;
            background-color: white;
            background-color: black;
        }
    </style>
</head>

<body>
    <div class="forget_from_container" id="forget_form_container">
        <h1>
            Forget Password
        </h1>

        <form id="forget_form">
            <input type="email" id="email" required placeholder="Your Email">
            <button>
                Send Email
            </button>
        </form>
    </div>
</body>
<script>
    
    document.getElementById("forget_form").addEventListener("submit", async (event) => {
        event.preventDefault()
        const email=document.getElementById("email").value
        const res=await fetch(`/email/${email}/forget`)
        const data=await res.json()
        if(data.status==false){
            alert(data.message)
        }
    })
</script>

</html>